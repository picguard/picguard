name: Test Windows zip (pro arm64)

on:
  workflow_dispatch:

# Declare default permissions as readonly.
permissions: read-all

jobs:
  zip:
    name: Create zip
    runs-on: windows-11-arm
    env:
      Platform: arm64
    steps:
      - uses: actions/checkout@v7

      - name: Setup Flutter
        shell: pwsh
        run: .\scripts\install-flutter-github-actions.ps1

      - name: Download a fresh binary instead of using the stamped x64 version
        shell: pwsh
        run: |
          Remove-Item -Path "$env:FLUTTER_ROOT\flutter\bin\cache\engine-dart-sdk.stamp" -Force -ErrorAction SilentlyContinue
          dart --version
          flutter precache --windows

      - name: Flutter doctor -v
        run: flutter doctor -v

      - name: Configure .env
        run: |
          $bytes = echo $env:ENV_KEY | %{[Convert]::FromBase64String($_)}
          [IO.File]::WriteAllBytes("$pwd\.env", $bytes)
        env:
          ENV_KEY: ${{ secrets.ENV_STG_BASE64_WINDOWS }}

      - name: copy eula-pro.rtf to the windows folder
        run: Copy-Item -Path "$pwd\macos\packaging\base\eula-pro.rtf" -Destination "$pwd\windows\LICENSE.rtf" -Force

      - name: Install fastforge
        run: dart pub global activate fastforge

      - name: Update config
        shell: pwsh
        run: |
          Copy-Item -Path "$pwd\windows\pro\CMakeLists.txt" -Destination "$pwd\windows\CMakeLists.txt" -Force
          Copy-Item -Path "$pwd\windows\pro\main.cpp" -Destination "$pwd\windows\runner\main.cpp" -Force
          Copy-Item -Path "$pwd\windows\pro\Runner.rc" -Destination "$pwd\windows\runner\Runner.rc" -Force
          Copy-Item -Path "$pwd\windows\pro\app_icon.ico" -Destination "$pwd\windows\runner\resources\app_icon.ico" -Force
          Copy-Item -Path "$pwd\windows\pro\LICENSE.rtf" -Destination "$pwd\windows\LICENSE.rtf" -Force

      - name: Install Flutter dependencies
        run: flutter pub get

      - name: Remove compiler options deprecated in VS 2026.
        run: |
          $file = "$pwd\windows\flutter\ephemeral\.plugin_symlinks\permission_handler_windows\windows\CMakeLists.txt"
          (Get-Content $file) | Where-Object { $_ -notmatch 'target_compile_options\(\$\{PLUGIN_NAME\} PRIVATE /await\)' } | Set-Content $file

      - name: Build zip
        shell: pwsh
        run: |
          $env:SENTRY_NATIVE_BACKEND = "breakpad"
          $env:FLUTTER_WINDOWS_PLATFORM = "$env:Platform"
          fastforge package `
            --skip-clean `
            --flutter-build-args "dart-define-from-file=.env,target=lib/main_pro.dart" `
            --platform windows `
            --targets zip `
            --artifact-name "{{name}}-pro-{{build_name}}.{{build_number}}-{{platform}}-portable-$env:Platform.{{ext}}"

      - uses: actions/upload-artifact@v7
        with:
          name: windows-zip-pro-artifact
          path: .\dist\**\*.zip

      - name: Clean up
        if: ${{ always() }}
        run: |
          if (test-path -Path "$pwd\.env") {
            echo "Remove file($pwd\.env)"
            remove-item -Path "$pwd\.env" -Force -ErrorAction SilentlyContinue
          } else {
            echo "File($pwd\.env) doesn't exist."
          }
